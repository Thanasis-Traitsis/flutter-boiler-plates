import 'package:flutter/material.dart';

// Buttons
const double borderRadiusButton = 5.0;
const EdgeInsets buttonSpacing = EdgeInsets.symmetric(vertical: 12, horizontal: 24);

// Containers
const double borderRadius = 10.0;
const double gap = 10.0;

// Screen Spacing
const EdgeInsets screenPadding = EdgeInsets.symmetric(horizontal: 10);
